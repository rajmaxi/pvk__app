import { StyleSheet } from 'react-native';
const productInfo = StyleSheet.create({
  buttonStyle: {
    backgroundColor: '#ff9000',
    borderWidth: 0,
    color: '#FFFFFF',
    borderColor: '#ff9000',
    height: 40,
    textAlign: 'center',
    justifyContent: 'center',
    textAlignVertical: 'center',
    fontSize: 14,
    textTransform: 'uppercase',
  },
  progressZero: {
    color: '#000',
    marginTop: 10
  },
  buttonStyleaddcart: {
    color: '#000',
    fontFamily: '',
    textTransform: 'uppercase',
    letterSpacing: .5,
    textAlign: 'center'
  }
});

export default productInfo;